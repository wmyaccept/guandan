# 掼蛋桌 · 给网站管理员

这不是静态网页，不能直接丢进现有网站目录。它是 Node.js 程序，需要在服务器上常驻运行，再用 Nginx 把一个**二级域名**转到这个程序。

现有网站保持不动。推荐访问地址：

```text
https://guandan.你的域名
```

## 你需要准备什么

1. 一台已经在跑网站的 Linux 服务器（Ubuntu / Debian 最省事）。
2. 能 SSH 登录，有 sudo。
3. 已有 Nginx（或同等反代）。
4. 域名可以加一条 DNS：
   - 类型 `A`
   - 主机记录 `guandan`
   - 值：这台服务器的公网 IP
5. Node.js 18 或以上。没有就装 22：

```bash
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs
node -v
```

## 安装

把压缩包解压到任意目录，例如 `/home/ubuntu/guandan` 或 `/opt/guandan`，然后：

```bash
cd /解压后的目录
chmod +x deploy/install.sh
./deploy/install.sh
```

脚本会安装依赖、写成开机自启。默认监听本机 `3000` 端口，不对公网开放。

## Nginx（不要覆盖现有站点）

只新增一个站点文件，不要改原来的 `default` 或现有域名配置。

1. 复制模板：

```bash
sudo cp deploy/nginx.subdomain.conf /etc/nginx/sites-available/guandan
sudo nano /etc/nginx/sites-available/guandan
```

2. 把 `guandan.example.com` 改成真实二级域名。
3. 启用并重载：

```bash
sudo ln -sf /etc/nginx/sites-available/guandan /etc/nginx/sites-enabled/guandan
sudo nginx -t
sudo systemctl reload nginx
```

4. 有 HTTPS 时再签一张证，不要动原来的证书：

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d guandan.你的域名
```

配置里已经带了 WebSocket 升级。少了 `Upgrade` 头，页面能开、进房会掉线。

## 验证

```bash
curl -I http://127.0.0.1:3000/health
sudo systemctl status guandan
```

浏览器打开 `http://guandan.你的域名`，能看到开桌页面即可。

## 日常

| 动作 | 命令 |
| --- | --- |
| 看日志 | `sudo journalctl -u guandan -n 50 --no-pager` |
| 重启 | `sudo systemctl restart guandan` |
| 停掉 | `sudo systemctl stop guandan` |
| 更新代码 | 覆盖解压后执行 `npm install --omit=dev && sudo systemctl restart guandan` |

## 不要这样做

- 不要解压到现有网站的 `www` / `html` 目录。
- 不要把 `server_name` 写成原来的主域名。
- 不要用宝塔「纯静态」或 PHP 运行环境硬套。
- 不要只开放 3000 端口给公网；走 80/443 反代即可。

端口冲突时，改 systemd 服务里的 `PORT`，Nginx 反代目标一起改。
